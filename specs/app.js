(function () {

    'use strict',
    define([
        'utils/simpleMath.spec'
    ], function () {

        var initialize = function () {

        };
        return {
            start: initialize
        };
    })
})();